# Required: put your existing Zhipu key here (can be the same old key).
ZHIPU_API_KEY=PLACEHOLDER_API_KEY

# Optional: proxy port for local development.
PROXY_PORT=8787

# Optional: set only when frontend and backend are not under same origin.
# VITE_BACKEND_API_BASE_URL=http://127.0.0.1:8787
